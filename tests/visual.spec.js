const{test,expect}=require('@playwright/test');test('tela permanece presa à viewport',async({page})=>{await page.goto('/');await page.waitForFunction(()=>window.CyberGame);const r=await page.evaluate(()=>({iw:innerWidth,ih:innerHeight,b:document.body.getBoundingClientRect().toJSON(),v:document.getElementById('viewport').getBoundingClientRect().toJSON(),meta:document.querySelector('meta[name=viewport]').content}));expect(Math.abs(r.v.width-r.iw)).toBeLessThanOrEqual(1);expect(Math.abs(r.v.height-r.ih)).toBeLessThanOrEqual(1);expect(r.meta).toContain('user-scalable=no');expect(r.meta).toContain('maximum-scale=1')});test('HUD e slots ficam dentro da tela',async({page})=>{await page.goto('/');for(const sel of ['.hud-top','.slots-container','.debug-dock']){const b=await page.locator(sel).boundingBox();expect(b.x).toBeGreaterThanOrEqual(0);expect(b.y).toBeGreaterThanOrEqual(0);expect(b.x+b.width).toBeLessThanOrEqual((await page.evaluate(()=>innerWidth))+1);expect(b.y+b.height).toBeLessThanOrEqual((await page.evaluate(()=>innerHeight))+1)}});